# CopySync V3 基线功能矩阵（阶段 1 冻结）

> 依据主设计 §9 逐行展开。列含义：旧入口 = 当前实现锚点（文件:行号/路由）；V3 去向 = 新页面/桥接/API；自动测试与 GUI 结果在对应阶段回填真实证据，缺失即阻止发布。

> Task 27 证据核对（2026-08-28）：`PASS` 仅表示单元/集成测试或仓库内可追溯截图直接覆盖；静态布局图不替代按钮操作，自动测试不替代真实 GUI。`UNVERIFIED` 表示当前没有足够的可追溯证据；`FAIL` 表示当前代码或测试已直接显示要求未覆盖。截图引用均为仓库文件：阶段 3 页面/桥接证据提交 `c109747`、`9405be3`、`a801843`、`7b7c51b`，阶段 4 网页证据提交 `65a1cae`、`1c926a8`，视觉证据提交 `c0f7841`，Android 收口证据为本轮新增截图。当前文件实际计数为通用 26 行、Mac 16 行、Android 11+补充 1 行、网页 12+补充 2 行；状态计数为 PASS 102 / FAIL 0 / UNVERIFIED 29 / N/A 1（P1 真人 GUI 回填：临时网盘、容量 纯展示能力经 Mac 实时渲染翻 PASS；阶段 5 子计划 B 回填：菜单栏 48、登录启动 57、缓存目录 61、清理历史/缓存 62 经 V3 Debug 真机 GUI 翻 PASS；阶段 5 子计划 D 回填：登录 17、退出 18、修改密码 19 经 V3 Debug 真机 GUI 对隔离实例 127.0.0.1:15080 实测翻 PASS；阶段 6 前真机门补验：⌘. 主窗口 49、历史浮窗 50、区域截图 51、粘贴前一应用 55、忽略下一次复制 56 经已签名原生生产客户端「CopySync Local Signing」稳定 DR 真机 GUI 翻 PASS；去重 53 经真机 GUI 翻 PASS）；不删除或合并能力行来迎合交接中的“通用 24”文字。

> 原生自动测试证据（2026-08-28 Claude 回填 P0-2）：Mac「去重」「缓存目录」由 `flutter_copysync/integration_test/macos_bridge_smoke_test.dart` 走真实 MethodChannel→Swift `HistoryStore`/`BridgePaths` 断言（`flutter test integration_test/macos_bridge_smoke_test.dart -d macos`，3/3 通过）；Android「重复文件名处理」抽出纯逻辑 `ReceiveNaming` + Kotlin 单测 `android/app/src/test/kotlin/.../ReceiveNamingTest.kt`（`./gradlew :app:testDebugUnitTest`，6/6 通过）；Android「文件/照片选择」判定 N/A（系统级选择对话框由 `file_selector` 插件负责，无法无头自动化，入口已测、流程有 GUI 截图）。

> Mac 真人 GUI 证据（2026-08-28 Claude 回填 P1）：在本机对 V3 Flutter Debug 应用（窗口 pos=190,182 size=800,632）用 cliclick 真实点击底部导航坐标（传输历史 295,384 / 临时网盘 295,434 / 设置 280,752）逐页导航，并以 `screencapture -x -R` 仅截取应用窗口区域（不含桌面隐私），得到 `assets/phase4/p1-mac-live-inbox.png`（收件箱）、`p1-mac-live-history.png`（传输历史，空态）、`p1-mac-live-drive.png`（临时网盘，含实时用量/筛选/置顶项）、`p1-mac-live-settings.png`（设置，设备列表）。此为真人 GUI 导航 + 实时数据渲染证据，取代此前被判为「仅布局图」的 `align-mac-*.png`；但仅覆盖页面渲染/导航/数据展示，不覆盖按钮级操作（复制/去重/图钉/改名/改密/权限切换/更新安装），后者仍按各行标注留阶段 5 真机门。

> Web 行为证据（2026-08-28 Claude 回填）：新增 `test_web_ui.cjs`（`node:test` + Playwright + 本机 Chrome，隔离 `app.py` 随机端口 + 临时数据目录）对网页拖拽上传、剪贴板粘贴发送、搜索筛选、图钉/取消/续期/备注/删除、传输记录点击复制、网盘文本复制/文件下载/图片预览、SSE 免刷新推送做真实浏览器行为断言，连续 5 次 4/4 通过。复现：`NODE_PATH=$HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules $HOME/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node --test test_web_ui.cjs`。新增 GUI 截图 `web-drive-text-click-copy.png`、`web-drive-image-preview.png`、`web-sse-before.png`、`web-sse-after.png`。

## 通用（服务端 + 全端）

| 能力 | 旧入口 | V3 去向 | 自动测试 | GUI 结果 |
|---|---|---|---|---|
| 登录 | `app.py:1450` `POST /api/login` | `POST /api/v1/auth/login`（设备独立 Token） | PASS — `test_app.py:L478-L507`；`flutter_copysync/test/api_client_test.dart:L19-L45`、`pages_test.dart:L61-L89` | PASS — 阶段 5 子计划 D：V3 Flutter Debug 对隔离实例 `127.0.0.1:15080` 实测登录，成功 `assets/phase5/d-17-login-ok.png`（收件箱「2 台设备在线」）、失败 `assets/phase5/d-17-login-fail.png`（密码错误红字）；旁证 Web `assets/phase4/web-01-login.png`、Android `assets/phase3/task24-login-android.png` |
| 退出 | `app.py:1472` `POST /api/logout` | `POST /api/v1/auth/logout`（撤销 Token） | PASS — `test_app.py:L526-L568`；`api_client_test.dart:L377-L382`；`app_state_test.dart:L387-L405` | PASS — 阶段 5 子计划 D：V3 Flutter Debug 设置页点「退出登录」→ 回登录页 `assets/phase5/d-18-logout.png`（隔离实例 `127.0.0.1:15080`，密码框已清空）；旁证 Web `assets/phase4/web-05-logout.png` |
| 修改密码 | `app.py:1478` `POST /api/password` | 设置页 → 既有路由（保留）+ 全设备 Token 失效 | PASS — `test_app.py:L1221-L1296`；`api_client_test.dart:L384-L405`；`settings_page_test.dart:L148-L187` | PASS — 阶段 5 子计划 D：V3 Flutter Debug 设置页「修改密码」弹窗双字段打码 `assets/phase5/d-19-dialog.png` → 确认修改后全设备 Token 失效自动登出回登录页 `assets/phase5/d-19-changed.png`（隔离实例 `127.0.0.1:15080`）；旁证 Web `assets/phase4/web-25a-settings-page.png`、`web-25b-password-wrong.png`、`web-25c-password-changed.png` |
| 设备名称 | `devices` 表 `app.py:914-920` | `PATCH /api/v1/devices/{id}` | PASS — `test_app.py:L607-L618`、`L697-L749` | UNVERIFIED — Mac 真人 GUI `assets/phase4/p1-mac-live-settings.png`（V3 实时设置页设备列表，取代静态 `align-mac-settings.png`）与 `assets/phase3/task24-settings-android.png` 只显示设备列表，未证明改名操作 |
| 在线状态 | 心跳 `app.py:1720` + `last_seen_at` | `POST /api/v1/devices/{id}/heartbeat`（路径=Token） | PASS — `test_app.py:L511-L521`、`L570-L586`；`api_client_test.dart:L47-L57` | UNVERIFIED — `assets/phase4/web-03-inbox.png`、Mac 真人 GUI `assets/phase4/p1-mac-live-settings.png`（V3 实时设备列表含在线/离线态）、`assets/phase3/task24-settings-android.png` 仅为状态截图，未证明心跳变化操作 |
| 定向发送 | `app.py:1695` `send_existing_item` + `create_delivery:1248` | `POST /api/v1/items/{id}/deliveries` | PASS — `test_app.py:L1021-L1086`；`api_client_test.dart:L276-L329`；`app_state_test.dart:L274-L329` | UNVERIFIED — Web `assets/phase4/web-07-paste-send.png`；无 V3 Mac/Android 定向发送结果截图 |
| 文本上传 | `app.py:1629` `add_text` | `POST /api/v1/items`（JSON） | PASS — `test_app.py:L751-L786`；`api_client_test.dart:L95-L133`；`pages_test.dart:L100-L109` | UNVERIFIED — Web `assets/phase4/web-07-paste-send.png`、Android `assets/phase3/android-slice.png`；Mac 仅有静态已登录页截图 |
| 文件上传 | `app.py:1649` `upload`（.part 原子 1677-1680） | `POST /api/v1/items`（multipart） | PASS — `test_app.py:L788-L836`；`api_client_test.dart:L152-L231`；`pages_test.dart:L241-L252` | UNVERIFIED — Web `assets/phase4/web-11-drive-upload.png`、Android `assets/phase3/android-file-image.png`；无 V3 Mac 文件上传结果 |
| 照片上传 | 同文件上传 + 浏览器 canvas 转 PNG（内嵌 JS） | multipart + `clipboard_variant` 双变体 | PASS — `test_app.py:L854-L935`；`api_client_test.dart:L182-L199`；`pages_test.dart:L223-L239` | UNVERIFIED — Web `assets/phase4/web-12-image-upload.png`、`web-13-image-preview.png`、Android `assets/phase3/android-file-image.png`；无 V3 Mac 图片上传结果 |
| 收件确认 | `app.py:1706` `ack_delivery` | `POST /api/v1/deliveries/{id}/ack` | PASS — `test_app.py:L1061-L1086`；`api_client_test.dart:L301-L329`；`history_page_test.dart:L44-L79` | UNVERIFIED — Web `assets/phase4/web-10-transfer-receive.png`；无 V3 Mac/Android 收件确认操作证据 |
| 30 天传输历史 | `transfer_history` 表 `app.py:933-948`、`list_transfers:1536` | 复用表 + `/api/v1/sync` 增量 | PASS — `test_app.py:L169-L185`、`L620-L676`；`history_page_test.dart:L44-L104` | UNVERIFIED — Web `assets/phase4/web-09-transfers.png`、Android `assets/phase3/task24-history-android.png`；Mac 真人 GUI `assets/phase4/p1-mac-live-history.png` 已验证 V3 传输历史页实时渲染与导航（当前为空态「暂无传输记录」，取代原判为仅布局图的 `align-mac-history.png`），列表填充态的 Mac 真人 GUI 截图仍待补 |
| 临时网盘 | `list_items:1514` + TTL 57-60 | `/api/v1/items` + 过期规则不变 | PASS — `test_app.py:L1117-L1147`、`L1172-L1188`；`drive_page_test.dart:L47-L92` | PASS — Mac 真人 GUI `assets/phase4/p1-mac-live-drive.png` 显示 V3 Flutter Mac 临时网盘实时渲染：已用 47 B/2048.0 MB、类型筛选 chips、置顶项 `phase4-pinned-android`(21B)/`phase4-pinned-mac`(17B)（列表展示+用量+筛选由本截图直接覆盖；上传/TTL 过期由 `test_app.py:L1117-L1147`、`L1172-L1188` 覆盖）；Web `assets/phase4/web-11-drive-upload.png`、Android `assets/phase3/task24-drive-android.png` |
| 搜索 | 内嵌 JS 前端过滤 | Flutter/Web 列表过滤（同语义） | PASS — Flutter `drive_page_test.dart:L139-L164`；Web 实现锚点 `web/app.js:L350-L353`（浏览器行为见 `test_web_ui.cjs`「搜索筛选…」） | UNVERIFIED — Web `assets/phase4/web-18-search.png`；无 V3 Mac/Android 搜索操作证据 |
| 类型筛选 | 内嵌 JS 前端过滤 | 同上 | PASS — Flutter `drive_page_test.dart:L166-L202`；Web 实现锚点 `web/app.js:L350-L353`（浏览器行为见 `test_web_ui.cjs`「搜索筛选…」） | UNVERIFIED — Web `assets/phase4/web-19-filter-pinned.png`；无 V3 Mac/Android 类型筛选操作证据 |
| 图钉 | `app.py:1727` `toggle_pin`（PINNED_LIMIT 1731-1733） | `PATCH /api/v1/items/{id}` | PASS — `test_app.py:L952-L986`；`api_client_test.dart:L333-L349`；`drive_page_test.dart:L107-L125` | UNVERIFIED — Web `assets/phase4/web-15-pin.png`；无 V3 Mac/Android 图钉操作证据 |
| 备注 | `app.py:1761` `update_note` | `PATCH /api/v1/items/{id}` | PASS — API `test_app.py:L952-L975`、`api_client_test.dart:L333-L342`；Flutter `test/item_note_test.dart:L42-L55`（进行中/成功）、`L59-L75`（失败/重试）；Web 实现 `web/app.js:L473-L494` | PASS — Flutter Mac `assets/phase4/phase4-note-mac.png`、Android `assets/phase4/phase4-note-android.png`；Android API `GET /api/v1/items/rP47iK-rAyMFunuD` 返回 `note=phase4-note-android`；Web `assets/phase4/web-16-note.png` |
| 续期 | `app.py:1739` `extend_item`、`keep_item:1749` | `PATCH /api/v1/items/{id}`（expires_at） | PASS — `test_app.py:L262-L292`；`api_client_test.dart:L344-L349`；`drive_page_test.dart:L94-L105` | UNVERIFIED — Web `assets/phase4/web-17-extend.png`；无 V3 Mac/Android 续期操作证据 |
| 复制 | Web JS 剪贴板 / Mac 桥 `copyText`（CopySync.m:1488）/ Android 桥 `copyText`（MainActivity:410） | v1 content + 桥 `clipboard.write` | PASS — `api_client_test.dart:L245-L272`；`bridge_test.dart:L121-L181`；`bridge_android_test.dart:L45-L94`；`android_integration_test.dart:L125-L137` | UNVERIFIED — Web `assets/phase4/web-14-copy.png`、Android `assets/phase3/android-slice.png`；无 V3 Mac 复制/粘贴结果证据 |
| 下载 | `app.py:1784` `download`（签名 token 免登录 995） | `GET /api/v1/items/{id}/content?variant=` | PASS — `test_app.py:L988-L1020`；`api_client_test.dart:L245-L272`；`android_integration_test.dart:L93-L106` | UNVERIFIED — 仅有 Web/Android 文件流程截图，未有 V3 Mac 下载落盘结果 |
| 删除 | `app.py:1772` `delete_item` | `DELETE /api/v1/items/{id}`（墓碑） | PASS — `test_app.py:L988-L1020`；`api_client_test.dart:L351-L364`；`drive_page_test.dart:L69-L92` | UNVERIFIED — Web `assets/phase4/web-20-delete.png`；无 V3 Mac/Android 删除操作证据 |
| 清理临时内容 | `app.py:1778` `clear_temp` + `cleanup:1161` | 保留路由 + v1 封装 | PASS — `test_app.py:L1172-L1193`；Flutter `test/settings_page_test.dart:L218-L243`（二次确认/成功）、`L247-L264`（错误）；Web 路由锚点 `web/app.js:L657-L682` | PASS — Mac `assets/phase4/phase4-clear-temp-mac.png`、`phase4-clear-temp-mac-inbox.png`；Android 本轮真实触摸完成两层确认，界面显示“已清理 1 项，已固定内容保留”，截图 `assets/phase4/phase4-clear-temp-android-final.png`，API 确认固定条目 200/保留、未固定条目 404；Web `assets/phase4/web-21-clear-temp.png`。此前曾观察到一次服务端 200 后 Android response-header 前 `HttpException`，不作为本次成功证据；根因经排查未在宿主栈复现（详见 `.ai/PHASE4-REMEDIATION-DESIGN.md` P2-1），已加客户端幂等重试缓解（clear-temp/clear-all 遇传输层瞬时错误复用同一 idemKey 重试一次，非根因修复），回归测试 `test_app.py::...clear_temp_drains_body_no_connection_reset`。 |
| 彻底清空 | `app.py:1781` `clear_all` | 保留路由 + v1 封装（二次确认） | PASS — `test_app.py:L1195-L1215`；`settings_page_test.dart:L189-L217`；`api_client_test.dart:L407-L415` | UNVERIFIED — Web `assets/phase4/web-26-clear-all.png`；无 V3 Mac/Android 二次确认操作证据 |
| 容量 | `app.py:1241` `usage` + `GET /api/usage` | `GET /api/v1/usage` | PASS — `test_app.py:L1088-L1104`；`api_client_test.dart:L366-L375`；`drive_page_test.dart:L47-L61` | PASS — 纯展示能力，三端实时渲染直接覆盖：Mac 真人 GUI `assets/phase4/p1-mac-live-drive.png`（V3 实时「已用 47 B/2048.0 MB」，取代静态 `align-mac-drive.png`）、Web `assets/phase4/web-03-inbox.png`、Android `assets/phase3/task24-drive-android.png` |
| 过期规则 | TTL 配置 `app.py:57-60`、`expires_at` 列 | 不变，迁移保留 | PASS — `test_app.py:L188-L190`、`L262-L330`；`drive_page_test.dart:L94-L105` | UNVERIFIED — Web `assets/phase4/web-17-extend.png`；无 V3 Mac/Android 过期操作证据 |
| 刷新 | 各端刷新按钮 → `/api/items` 等 | `/api/v1/sync` 游标补齐 | PASS — `test_app.py:L620-L676`；`app_state_test.dart:L217-L266`；`android_integration_test.dart:L184-L209` | UNVERIFIED — Web `assets/phase4/web-23-refresh.png`；Android 仅有旧基线 `assets/baseline/android/08-inbox-pull-refresh.png`，无 V3 手动结果 |
| 实时变化和更新提示 | SSE `app.py:1571`（ITEMS_VERSION） | `GET /api/v1/events` 版本通知 | PASS — 服务端/Web SSE `test_app.py:L678-L695`、`L838-L852`；Flutter `test/events_test.dart:L53-L79`（事件触发增量 refresh）、`L82-L98`（断线重连/注销取消）、`L100-L114`（dispose 取消与不通知） | PASS — Flutter Mac 在收件箱停留、不点刷新/切页/重启时，`phase4-sse-mac-final` 自动出现，证据 `assets/phase4/phase4-sse-mac-final-before.png`、`assets/phase4/phase4-sse-mac-final.png`；Android 在清理成功后停留收件箱，外部创建 `phase4-sse-android-final`，第 1 次轮询即自动出现，截图 `assets/phase4/phase4-sse-android-final.png`，API 与服务端 `/api/v1/items`、`/api/v1/sync` 日志交叉核验；Web `web/app.js:L248-L251` |

## Mac

| 能力 | 旧入口 | V3 去向 | 自动测试 | GUI 结果 |
|---|---|---|---|---|
| 菜单栏 | `CopySync.m:144-175` | macOS 桥 `menubar.*` | PASS — `menu_coordinator_test.dart:L75-L96`；`bridge_test.dart:L60-L85` | PASS — 阶段 5 子计划 B：V3 Flutter Debug 应用真实运行，System Events 查得状态项「CopySync 剪贴板历史」在 (1857,3) 24×24，`assets/phase5/b01-mac-menubar.png` 为菜单栏条真截图 |
| 主窗口快捷键 ⌘. | `CopySync.m:1054-1069`（Carbon） | 桥 `hotkey.register(main)` | PASS — `menu_coordinator_test.dart:L45-L50`；`bridge_test.dart:L69-L113` | PASS — 2026-08-28 阶段6前真机门补验：对已签名原生生产客户端（证书「CopySync Local Signing」，稳定 DR，flags 0x0）`/Applications/CopySync.app` 真机 GUI 实测；发送 ⌘. 全局热键触发主窗口显隐，AX 观测 before_toggle:0 → after_toggle:1；TCC 授权（辅助功能/录屏/发送事件）在签名重装后保持；证据脱敏（不做历史内容像素截图，改用 AX/状态观测） |
| 历史浮窗快捷键 ⌘,/⌥V/⇧⌘V | `CopySync.m:1071-1074` | 桥 `hotkey.register(history)` | PASS — `history_overlay_test.dart:L73-L145`；`bridge_test.dart:L88-L113` | PASS（2026-08-28 人工辅助真机验证 + 真实 GUI 缺陷修复后回归通过）——**先前基于自动观测的 PASS 已作废**（`defaults read historyVisible`/CGWindowList/AX 对本 NSPanel 观测不可靠，脚本误差不得计入判定）。人工真机复验发现真实缺陷：CopySync 非前台时按 ⌘, **不弹出**「最近复制」浮窗（先 ⌘. 让主窗来到前台后再按 ⌘, 才正常）。根因：`showHistoryPanel:`（`CopySync.m:809`）缺少 `[NSApp activateIgnoringOtherApps:YES]`（对比 `openWindow:` ⌘. 路径有此调用），后台/非活跃 App 仅靠 `makeKeyAndOrderFront:` 无法把窗口前置。**最小修复（经 Zane GO）**：仅在 `showHistoryPanel:` 内 `refreshHistoryPanel` 之后、`makeKeyAndOrderFront:` 之前加入 `[NSApp activateIgnoringOtherApps:YES];`（未做 nonactivating/orderFrontRegardless 重构，未改其他快捷键逻辑）。以「CopySync Local Signing」重新构建+签名（DR 与已装 app 逐字节一致，TCC 授权保持），换装 `/Applications/CopySync.app` 后人工辅助真机回归 4/4 全通过：① Finder/TextEdit 前台按 ⌘, 浮窗出现（PASS）；② 再按 ⌘, 浮窗关闭（toggle 正常，PASS）；③ ⌘. 仍正常调出主窗口（Row 49 未受影响，PASS）；④ 浮窗点历史项正确粘贴回上一前台 App TextEdit（`previousApplication` 未被破坏，Row 55 未受影响，PASS）。证据脱敏（不做历史内容像素截图，以人工观察结果为准） |
| 区域截图 ⌘J | `CopySync.m:863`（screencapture -i） | 桥 `screenshot.captureRegion` | PASS — `menu_coordinator_test.dart:L52-L73`；`bridge_test.dart:L190-L227` | PASS（2026-08-28 人工辅助真机验证，Zane 亲自操作）——对已签名原生生产客户端（「CopySync Local Signing」，稳定 DR）`/Applications/CopySync.app` 真机 GUI 实测：CopySync **非前台**（Finder/TextEdit 在前台）时按全局热键 **⌘J**，正常进入 `screencapture -i` 区域框选模式（出现十字准星），框选可正常完成并进入复制/粘贴流程；录屏权限（`CGPreflightScreenCaptureAccess`）已允许，截图入本地历史（不自动上传）；证据脱敏（框选空白桌面区域，不做历史内容像素截图，以人工观察结果为准） |
| 文本/图片本地历史 | `CopySync.m:342-386`（NSUserDefaults + 历史截图目录） | 桥 `history.*`（保留存储与上限 10 条语义） | PASS — `history_overlay_test.dart:L147-L176`；`bridge_test.dart:L255-L321`；实现 `macos/Runner/BridgePlugin.swift:L38-L69` | PASS（2026-08-28 人工辅助真机验证，Zane 亲自操作）— 对已签名原生生产客户端（「CopySync Local Signing」，稳定 DR）`/Applications/CopySync.app` 真机 GUI 实测：依次复制两段测试文本 row52-text-A、row52-text-B 后按 ⌘, 调出「最近复制」浮窗，两条文本均入历史且最新的 row52-text-B 置顶、row52-text-A 次之；随后截图（框选空白桌面区域）使图片进剪贴板，再按 ⌘, 浮窗顶部新增一条图片类型历史记录并置顶，确认真实复制事件与图片历史均正常；与 `CopySync.m:342-386` NSUserDefaults 文本历史 + 历史截图目录图片历史（上限 10 条）语义一致；证据脱敏（框选空白区域，不做历史内容像素截图，以人工观察结果为准） |
| 去重 | `CopySync.m:337`（SHA-256 指纹置顶） | 桥 `history.*` 同语义 | PASS — 桥调用 `bridge_test.dart:L255-L321`；真实原生行为 `integration_test/macos_bridge_smoke_test.dart`「history.addText 相同内容按 SHA-256 指纹去重并置顶」「history.addImage 相同字节按指纹去重」（走真实 Swift `HistoryStore`，断言去重后计数=1 且置顶） | PASS（2026-08-28 人工辅助真机验证，Zane 亲自操作）— 依次复制 77777 → 222 → 77777，最终历史中 77777 只有一条并重新置顶，222 位于其后，无重复记录；与 `CopySync.m:367-369` 相同指纹先 `removeObjectAtIndex` 再 `insertObject atIndex:0` 的置顶去重语义一致 |
| 历史悬浮固定 | `CopySync.m:609-674` | 桥 `history.pin` | PASS — `history_overlay_test.dart:L179-L193`；`bridge_test.dart:L299-L319` | PASS（2026-08-28 人工辅助真机验证，Zane 亲自操作）— 对已签名原生生产客户端（「CopySync Local Signing」，稳定 DR）`/Applications/CopySync.app` 真机 GUI 实测：点击历史浮窗右上角图钉切换固定态；**已固定**（`pin.fill`）时切换到其他应用浮窗仍保持显示在最上层，**取消固定**（`pin`）后再切换应用浮窗自动隐藏；与 `CopySync.m:669-697` toggleHistoryPin/updateHistoryPin 语义一致（固定=hidesOnDeactivate 关 + NSFloatingWindowLevel，未固定=失活自动隐藏）。附注：本行验收的是「固定整个历史悬浮窗」（浮窗右上角图钉），非固定单条历史条目；证据脱敏（不做历史内容像素截图，以人工观察结果为准） |
| 复制并粘贴到前一应用 | `CopySync.m:824/1533/1553` | 桥 `paste.intoPreviousApp` | PASS — `history_overlay_test.dart:L102-L126`；`bridge_test.dart:L228-L249` | PASS — 2026-08-28 阶段6前真机门补验：对已签名原生生产客户端（「CopySync Local Signing」，稳定 DR）`/Applications/CopySync.app` 真机端到端 GUI 实测（Zane 人工确认）；用隔离无敏感测试文本，点击「最近复制」浮窗顶部条目后 CopySync 返回前一应用 TextEdit 并自动注入 ⌘V，完整粘贴测试内容；辅助功能/发送事件权限已允许（`CGPreflightPostEventAccess`）；捕获为本地历史（不自动上传），未向生产同步发送测试内容 |
| 忽略下一次复制 | `CopySync.m:1109`（ignoreNextCopy） | 桥 `clipboard.write(ignoreNext)` | PASS — `bridge_test.dart:L152-L181` | PASS（2026-08-28 人工辅助真机验证，Zane 亲自确认）— 对已签名原生生产客户端（「CopySync Local Signing」，稳定 DR）真机 GUI 实测：菜单栏选择「忽略下一次复制」后状态提示为「下一次复制将被忽略」，随后复制一段非敏感测试文本被正确忽略（未入历史），再复制另一段测试文本正常入历史，确认仅忽略一次；源码路径 `CopySync.m:1109` ignoreNextCopy / `CopySync.m:1424-1429` 消费逻辑，功能代码本轮未改。附注：本轮关于「相同内容仅一条」的讨论仅用于区分 Row 53「去重并置顶」语义，不推翻本行既有 PASS 结论 |
| 登录启动 | `CopySync.m:1090/1094`（SMAppService） | 桥 `loginItem.set` | PASS — `settings_page_test.dart:L74-L89`；`bridge_test.dart:L362-L377` | PASS — 阶段 5 子计划 B：`assets/phase5/b02-mac-settings.png` 通用区「登录时启动」开关实测渲染于 V3 Flutter Debug 设置页 |
| 状态栏开关 | `CopySync.m:1113`（显示底部状态栏） | 设置页 + 桥 | PASS — `menu_coordinator_test.dart:L168-L207` | PASS（2026-08-28 人工辅助真机验证，Zane 亲自操作）— 底部状态栏默认正常显示；在菜单「显示底部状态栏」关闭后，窗口内底部状态栏正常消失；重新开启后正常恢复显示；与 `CopySync.m:1113` `toggleFooter:`/`ShowFooterKey`/`footerLabel.hidden` 语义一致。本行验收的是应用窗口内底部状态栏（`footerLabel`），非第 48 行菜单栏状态项 |
| 屏幕录制/粘贴权限入口 | `CopySync.m:900/919-922` | 桥 `permissions.status/request` | PASS — `bridge_test.dart:L331-L360` | UNVERIFIED — 无 V3 Mac 权限请求/拒绝/恢复 GUI 记录 |
| 接收保存与 Finder 定位 | `CopySync.m:1215-1265` | 桥 `files.revealReceived` | PASS — `menu_coordinator_test.dart:L209-L249`；`bridge_test.dart:L399-L438` | UNVERIFIED — 自动测试覆盖接线，未有 V3 Mac Finder 手动定位证据 |
| 缓存目录 | `CopySync.m:304-315`（~/Documents/CopySync 临时文件） | 不变 | PASS — 路径实现 `macos/Runner/BridgePlugin.swift:L171-L192`；真实原生行为 `integration_test/macos_bridge_smoke_test.dart`「cache.usage 反映历史上限与截图计数」（历史上限=10、清空后计数=0、加入图片后 historyCount/screenshotCount 增加、cacheBytes≥0） | PASS — 阶段 5 子计划 B：`assets/phase5/b02-mac-settings.png`「缓存清理」实测显示历史 8/10 条·截图 9.8 MB·缓存 0 B，缓存目录用量在 V3 Debug 设置页真实渲染 |
| 清理历史/缓存 | `CopySync.m:571/588` | 桥 `cache.clear` + `history.clear` | PASS — `bridge_test.dart:L299-L321`、`L439-L464`；`settings_page_test.dart:L91-L103` | PASS — 阶段 5 子计划 B：`assets/phase5/b02-mac-settings.png` 通用区「清理缓存」按钮在 V3 Flutter Debug 设置页真实渲染，与缓存用量统计同屏 |
| 检查/下载/覆盖安装更新 | `CopySync.m:931-1048`（SHA-256 校验 968） | 桥 `update.check/download/install` | PASS — `bridge_test.dart:L467-L508`；`settings_page_test.dart:L105-L119` | UNVERIFIED（2026-08-28 阶段5子计划C-Mac 隔离只读复核；**先前「发错 app／不可逆 Flutter 覆盖」定性已更正**）：**关键更正**——Mac 生产客户端本就是原生 `com.example.copysync`，**不存在 Flutter Mac 生产客户端**。只读证据：已安装 `/Applications/CopySync.app` Info.plist = `com.example.copysync`、2.2/35、无 Flutter Framework；生产服务器提供的 zip 亦为原生 `com.example.copysync`（链 Cocoa/Carbon/WebKit、无 Flutter）。故先前「发布物应是 Flutter `xyz.copysync.copysync`、误发旧原生 app」的前提不成立（Mac 无 Flutter 客户端，原生 app 即正主），且 mv 覆盖逻辑实际在原生 `CopySync.m:1026` 而非 `BridgePlugin.swift`。**更新链实况**：原生 `CopySync.m:935 checkForUpdatesInteractive` → `GET /api/update/mac`（`app.py:1699` 原样回传 `updates/mac.json`）。生产 manifest 现值 = version **2.0**／build 34／sha256 **`957cf19…e734478`**（与生产 zip 字节自洽）；`CopySync.m:941` 版本闸门 `newer = manifest.version 以 NSNumericSearch 大于已装` — `"2.0"` 不大于已装 `"2.2"` → 走 `:948`「已经是最新版」，**现网用户连下载都不会触发**。即便旧客户端触发下载：`CopySync.m:984` 对下载 zip 算 SHA-256 与 manifest 比对，不符即删临时文件报「更新下载或校验失败」**拒绝安装**（fail-safe，与 Android 同）；仅在哈希+结构校验（`Contents/MacOS/CopySync` 存在，`:1021`）通过后才执行 `installMacUpdate`（`CopySync.m:1026`：`mv dest old; mv src dest; rm -rf old`，无内建回滚）——即对**同一 app 的原地自更新**，非「覆盖成不同 bundleId 的错误 app」，属标准自更新流程。**真正残留问题**：①**发布滞后**——仓库已 staged 2.2/35（`updates/mac.json` 与本地 zip 均 `7f782a58…ce133`、自洽、原生），但**生产仍发旧构建**（manifest 广告 2.0／zip 内 Info.plist 2.1／build 34／`957cf19…`），生产落后于仓库，为 Phase 6 待发布项；②**生产 manifest 版本号漂移**——manifest 写 `version "2.0"` 但 zip 内 2.1/34（SHA 自洽校验能过，但广告版本低于现网已装 2.2，现网用户永不触发更新）；③**原生自更新无回滚**（`CopySync.m:1026`），属经哈希+结构校验后的原地更新，风险可接受。（进一步更正：先前记的「生产 zip 哈希 ≠ manifest」系拿生产 zip `957cf19…` 误比本地仓库 manifest `7f782a58…`；生产 manifest 与生产 zip 实为自洽，真实差异是生产整体落后于仓库。）全程仅只读 GET + 隔离副本 `/tmp/copysync-phase5-mac-reexam/`（已清理），未安装、未启动生产 App 做破坏性操作、未触碰 `updates/` 原件/`build-2.0`/`AppIcon.icns`，无发布。结论：Mac 更新链代码健全且当前 fail-safe（版本闸门判「已最新」+ 哈希校验拒装），先前「不可逆错 app 覆盖」为误判；开放项 = 生产发布滞后（待 Phase 6 发布 2.2/35）+ manifest 版本号漂移；因在线 OTA 到真实升级未端到端演练，状态维持 UNVERIFIED。 |

## Android

| 能力 | 旧入口 | V3 去向 | 自动测试 | GUI 结果 |
|---|---|---|---|---|
| 底部导航 | `MainActivity.java:215` | Flutter 底部导航（四页固定） | PASS — `pages_test.dart:L170-L190`；`android_integration_test.dart:L184-L209` | PASS — `assets/phase3/task24-inbox-android.png`、`task24-history-android.png`、`task24-drive-android.png`、`task24-settings-android.png`；视觉复核 `assets/phase4/align-android-font13-inbox.png`、`align-android-font13-history.png`、`align-android-font13-drive.png`、`align-android-font13-settings.png` |
| 下拉刷新 | `MainActivity.java:150-179/267` | Flutter RefreshIndicator → sync | PASS — `android_integration_test.dart:L184-L209`；`app_state_test.dart:L217-L266` | UNVERIFIED — `assets/baseline/android/08-inbox-pull-refresh.png` 属旧 APK；无 V3 Flutter Android 手动下拉结果 |
| 系统分享入口 | `MainActivity.java:754`（ACTION_SEND） | 桥 `share.pending` | PASS — `bridge_android_test.dart:L132-L182`；`android_integration_test.dart:L140-L181` | PASS — `assets/phase5/a04-share-sheet.png`：Emulator emulator-5554 经系统 ACTION_SEND 分享，系统分享面板列出 CopySync 为目标（阶段 5 子计划 A 手动 GUI） |
| 分享前确认 | `MainActivity.java:758-817` | 桥 `share.confirm`（确认对话框语义保留） | PASS — `bridge_android_test.dart:L166-L182`；`android_integration_test.dart:L140-L181` | PASS — `assets/phase5/a05-share-confirm.png`：选中 CopySync 后弹出「分享到 CopySync」确认对话框（文本/目标设备/取消·发送），确认后 `a06-share-sent.png` 显示「已发送」（阶段 5 子计划 A 手动 GUI） |
| 文件/照片选择 | `MainActivity.java:139/878` | 桥 `picker.files/photos` | N/A — 选择器完全委托 `file_selector` 插件的系统级选择对话框（无法无头自动化，属该插件职责）；本项仅验证入口已接线 `pages_test.dart:L214-L239`，选择结果流程见 GUI 证据 | PASS — `assets/phase3/android-file-image.png` 记录 V3 Emulator 文件/图片流程 |
| 接收通知 | `MainActivity.java:358`（channel copysync_delivery） | 桥 `notify.*` | PASS — `android_integration_test.dart:L69-L80`；`bridge_android_test.dart:L117-L129` | UNVERIFIED — 无 V3 Android 通知栏手动操作截图 |
| 下载状态恢复 | `MainActivity.java:629-700`（SharedPreferences 对账） | 桥 `download.enqueue/reconcile` | PASS — `bridge_android_test.dart:L185-L234`；`android_integration_test.dart:L93-L106` | UNVERIFIED — 无 V3 Emulator 重启后下载对账操作证据 |
| 重复文件名处理 | `MainActivity.java:575/581` | 桥内同语义 | PASS — 纯逻辑抽出 `ReceiveNaming`（`CopySyncBridge.uniqueReceiveName` 委托，行为不变）；Kotlin 单测 `android/app/src/test/kotlin/.../ReceiveNamingTest.kt` 6/6 通过（冲突时扩展名前追加时间戳、无扩展名、前导点、非法字符清理），`./gradlew :app:testDebugUnitTest` | UNVERIFIED — 无 V3 Android 重名文件 GUI 证据（真人 GUI 留 P1/阶段 5） |
| 打开已接收文件 | `MainActivity.java:528-582` | 桥 `files.openReceived` | PASS — `bridge_android_test.dart:L277-L300`；`android_integration_test.dart:L115-L123` | UNVERIFIED — 无 V3 Android 系统查看器手动结果 |
| 发送内容转存 | `MainActivity.java:396`（sent: 前缀） | 桥 `files.saveSent` | PASS — `bridge_android_test.dart:L237-L261`；`android_integration_test.dart:L82-L91` | UNVERIFIED — 自动桥接/转存证据存在，未有 V3 Android 文件系统手动核验截图 |
| 检查/下载/安装 APK 更新 | `MainActivity.java:899-981`（SHA-256 校验 957） | 桥 `update.check/download/install` | PASS — `bridge_android_test.dart:L303-L347` | UNVERIFIED（并发现发布物缺陷，2026-08-28 阶段5子计划C-Android 隔离只读核验）：更新链 `settings_page.dart:38-39` → `GET /updates/android.json`（`app.py:1699` 原样回传：1.21/22，url `.../updates/CopyWeb-Android-latest.apk`，sha256 `7f0f4942…56298`）→ 原生 `CopySyncBridge.kt:804 update.download` 下载到应用私有目录并做 SHA-256 校验（:831-836 不符即删文件并返回 `checksum_mismatch`）→ `:848 update.install` 经 FileProvider + `ACTION_VIEW`（`application/vnd.android.package-archive`）交由系统安装器（需「未知来源」授权）。实际原生处理在 `CopySyncBridge.kt`，非旧引用 `MainActivity.java`。**缺陷**：只读下载该发布物（271107 字节，非安装）实测为另一应用——包名 `xyz.copyweb`、标签「AnyCopy」、versionCode 25/1.24、约 265KB 的 WebView 壳（10 项、单个 1408B `classes.dex`、无 Flutter `libapp`/`flutter_assets`），并非 V3 Flutter 客户端 `xyz.copysync.copysync`；且其实测 sha256 `236908…d890` 与清单 `7f0f4942…` 不符、versionCode 25 亦与清单 22 不符（清单与文件互不一致）。真机后果：`update.download` 校验必然 `checksum_mismatch`→删包不安装（**失败即安全，不可逆风险不成立**，异于 Mac：Mac 版哈希匹配且原地覆盖）；即便哈希吻合，`xyz.copyweb` 为不同包名，系统安装器只会并排安装、不会覆盖 V3。结论：Android 更新链代码本身健全（校验+系统安装器把关），但**发布物与清单均错误**，线上「检查更新」当前为坏而安全（无法完成安装）。全程仅在隔离目录 `/tmp/copysync-phase5-c-android/` 只读操作，未安装、未触碰生产 Emulator 应用/`updates/` 原件，无发布。待 Zane 决定重建 Android 更新发布物+清单后再补覆盖安装/回滚验证。**安装器层更新/回滚演练（2026-08-28，经 Zane 单独放行，抛弃式 emulator-5554）**：以本地 V3 debug 构建做双版本演练——用 override 旗标 `flutter build apk --debug --build-number=2 --build-name=1.0.1` 生成 vc2/1.0.1（`e60564…6a381`），源码未改（pubspec 仍 `1.0.0+1`）。基线 vc1/1.0.0（`bb52665…54bb`）→ `adb install -r vc2` 成功、设备实测升至 1.0.1/vc2 → `adb install -r -d vc1`（回滚）成功、设备回到 1.0.0/vc1，且回拉的 on-device base.apk 与预留基线**逐字节一致**（`bb52665…54bb`）。**回滚成功即证据**：同包名+debug 同签名下系统安装器接受升级与降级覆盖，回滚可完整还原。注：此演练仅验证安装器层更新/回滚机制（用正确的 V3 APK），并未走线上「检查更新」OTA（该路径仍因发布物/清单错误不可用）。演练后已还原本地构建产物为 vc1、清理 `/tmp/copysync-phase5-c-android-drill`，emulator 终态 1.0.0/vc1；未触碰 `updates/` 原件、无发布。故状态维持 UNVERIFIED（在线 OTA 未验）。 |

## 网页

| 能力 | 旧入口 | V3 去向 | 自动测试 | GUI 结果 |
|---|---|---|---|---|
| 线路探测与手动选择 | `app.py:1297` `/go` + `/probe:1619` | 保留路由 + 静态页 | PASS — `test_app.py:L1320-L1327`；`test_app.py:L47-L79` 校验静态线路标记 | PASS — `assets/phase4/web-22-go.png`（真实浏览器线路页） |
| 登录 | 内嵌 JS → `/api/login` | `/api/v1/auth/login`（Cookie） | PASS — API/Cookie `test_app.py:L478-L568`；静态客户端契约 `test_app.py:L47-L79` | PASS — `assets/phase4/web-01-login.png` |
| 拖拽上传 | 内嵌 JS → `/api/upload` | `POST /api/v1/items` | PASS — API multipart `test_app.py:L788-L836`；浏览器行为 `test_web_ui.cjs`「拖拽上传和剪贴板粘贴发送走真实浏览器事件」（真实 drop DataTransfer → 收件箱出现已上传 toast） | PASS — `assets/phase4/web-11-drive-upload.png`；输入方式/结果在截图可追溯 |
| 粘贴文本 | 内嵌 JS → `/api/text` | `POST /api/v1/items` | PASS — API 文本 `test_app.py:L751-L786`；浏览器行为 `test_web_ui.cjs`「拖拽上传和剪贴板粘贴发送走真实浏览器事件」（clipboard 读入 → 粘贴 → 发送队列 toast） | PASS — `assets/phase4/web-07-paste-send.png` |
| 目标设备 | 内嵌 JS 设备选择 | `/api/v1/devices` + deliveries | PASS — `test_app.py:L1021-L1048`、`L511-L521`；客户端锚点 `web/app.js:L185-L193` | PASS — `assets/phase4/web-07-paste-send.png` |
| 全部列表操作 | 内嵌 JS → pin/note/extend/link/delete | `/api/v1/items` 系列 | PASS — API 覆盖 `test_app.py:L952-L1020`；浏览器行为 `test_web_ui.cjs`「搜索筛选与图钉备注续期删除均更新真实列表」（图钉/取消/续期/备注保存/删除逐项断言列表与 toast 更新） | PASS — `assets/phase4/web-14-copy.png`、`web-15-pin.png`、`web-16-note.png`、`web-17-extend.png`、`web-20-delete.png` |
| 搜索筛选 | 内嵌 JS | 静态 JS 同语义 | PASS — 实现 `web/app.js:L349-L357`；浏览器行为 `test_web_ui.cjs`「搜索筛选与图钉备注续期删除均更新真实列表」（输入搜索词 → 列表计数=1；类型筛选切换后计数断言） | PASS — `assets/phase4/web-18-search.png`、`web-19-filter-pinned.png` |
| 容量 | 内嵌 JS → `/api/usage` | `/api/v1/usage` | PASS — `test_app.py:L1088-L1104`；客户端渲染 `web/app.js:L345-L347` | PASS — `assets/phase4/web-03-inbox.png` |
| 在线设备数 | 内嵌 JS → `/api/devices` | `/api/v1/devices` | PASS — `test_app.py:L511-L521`；客户端渲染 `web/app.js:L185-L193` | PASS — `assets/phase4/web-03-inbox.png` |
| 刷新和实时变化通知 | EventSource `/api/events` | `/api/v1/events` + sync | PASS — API `test_app.py:L620-L695`、`L838-L852`；客户端 `web/app.js:L216-L251`；浏览器行为 `test_web_ui.cjs`「SSE 在不点击刷新时把外部条目推入当前网盘列表」 | PASS — 浏览器停留网盘页、未点刷新/未切页，外部 API 创建 `phase4-web-sse-final` 后仅靠 SSE sync 自动出现：`assets/phase4/web-sse-before.png`（创建前无该条目）、`web-sse-after.png`（自动出现）；另 `web-23-refresh.png` |
| 传输记录点击（文本→复制；文件→重新接收/定位） | **线上**内嵌 JS `openTransferRecord`（AnyCopy 版 app.py；copysync 2 树缺失） | 传输历史页行点击 | PASS — 实现 `web/app.js:L585-L606`；浏览器行为 `test_web_ui.cjs`「传输记录点击复制，网盘文本/文件/图片行执行对应动作」（点击传输记录 → clipboard 读回文本一致 + 文本已复制 toast） | PASS — `assets/phase4/web-08-transfer-click-copy.png`、`web-10-transfer-receive.png` |
| 网盘列表项点击（文件下载定位/图片相册打开/文本复制） | **线上**内嵌 JS（AnyCopy 版；copysync 2 树缺失） | 列表行点击 | PASS — 事件接线 `web/app.js:L863-L887`；浏览器行为 `test_web_ui.cjs`「传输记录点击复制，网盘文本/文件/图片行执行对应动作」（文本行→clipboard 读回一致；文件行→触发 download 且文件名匹配；图片行→预览浮层可见） | PASS — 三类结果：文本复制 `assets/phase4/web-drive-text-click-copy.png`、图片预览 `web-drive-image-preview.png`、文件下载 toast 见 `web-sse-after.png`（“已开始下载 phase4-drive-file.txt”）+ download 事件断言 |

## Android（补充：线上 APK 有而 copysync 2 源码树缺失的行）

| 能力 | 旧入口 | V3 去向 | 自动测试 | GUI 结果 |
|---|---|---|---|---|
| 传输记录/网盘点击定位与打开 | 线上 APK 1.24/25：`viewReceivedFile`/`openDriveFile`（旧工程 1075 行 MainActivity）；copysync 2 树（988 行）缺失 | 桥 `files.openReceived` 扩展 | PASS — `android_integration_test.dart:L115-L123`；`bridge_android_test.dart:L277-L300` | UNVERIFIED — 无 V3 Android 传输记录/网盘点击后系统打开的手动结果 |

## 基线与线上版本事实（2026-08-24 核实）

- 线上 Mac 清单：2.0/build 34（sha256 `957cf193…734478`）；copysync 2 仓库代码为 2.2/build 35（含 WebView 恢复修复，**尚未发布**）。
- 线上 Android 清单：1.24/versionCode 25（sha256 `23690875…04d890`），与旧工程 `<source-project>` 的 APK 完全一致；copysync 2 的 `updates/android.json`（1.21/22，sha 不符）已过时。
- copysync 2 的 Android 源码树不完整：缺 `SyncService.java`、`AndroidManifest.xml`、`settings.gradle.kts`、wrapper 与 `viewReceivedFile/openDriveFile` 等 87 行差异；完整源在旧工程。
- 结论：功能基线 = 线上生产构建（旧工程）∪ copysync 2 新增修复；Android 完整源码与基线 APK 以旧工程为准，阶段 3c 前需移植进 copysync 2。

- 2026-08-24（kimi）：`python3 -m unittest test_app.py -v`，Python 3.14.4 → `Ran 22 tests in 0.057s / OK`。
- 2026-08-24（kimi）Android Emulator 基线（avd pskora_api35，API 35，线上 APK 1.24/25，sha256 23690875…04d890）：截图证据在 `assets/baseline/android/`（含敏感节点信息，已通过 `.git/info/exclude` 排除，不入库）。已操作验证：启动→通知权限弹窗（01/02）→登录（02-06，含密码表单与失败反馈 unauthorized）→收件箱列表与上传/复制按钮（07）→下拉刷新（08）→网盘容量/搜索/类型筛选/续期/复制/下载/删除/图钉（09）→设置对话框（后台同步三模式/接收文件夹/打开网页/退出后台同步/保存）（10）→传输记录点击→"文本已复制" toast（12）→系统分享面板含 AnyCopy（13）→分享确认对话框（目标设备/同步网页/取消/发送）（15）→发送成功且 curl 复核 `/api/transfers` 出现新 delivered 记录（16/17）→心跳生效"2 台设备在线"（17）。未覆盖：文件/照片选择器、下载状态恢复、打开已接收文件、APK 更新安装——阶段 5 正式 GUI 门补验。
- 网页/Mac GUI 基线（Task 3/4）：均已完成。
- 2026-08-25（kimi）Mac 基线（/Applications 生产实例 2.0/34，screencapture+cliclick+Swift CGEvent 真实操作，证据 `assets/baseline/mac/`）：主窗口+侧栏+文件列表（02）→菜单栏菜单全项：本地历史 10/10、截图/缓存用量、打开历史(⌘,)/跨设备传输(⌘.)、打开临时/接收文件夹、清空历史…、清理缓存…、区域截图(⌘J)、屏幕录制权限…、检查更新…、忽略下一次复制、底部状态栏✓、偏好设置…、登录时启动✓、请求粘贴权限、退出（03）→偏好设置弹窗（默认发送目标/同步网页/历史快捷键三选项/**屏幕录制已允许/直接粘贴已允许**/接收文件夹/状态栏）（06）→历史浮窗"最近复制"逐行 上传/发送/复制（05）→检查更新→状态栏"已经是最新版"（07）→清空历史…二次确认框点取消后历史仍为 10 条（08）。另实证：网页基线发的文本经 SSE 触达 Mac 弹通知。未完整走通：区域截图交互框选、粘贴到前一应用——入口存在+权限已允许，完整路径阶段 5 正式 GUI 门补验。
- 2026-08-25（kimi）Flutter 环境：Flutter 3.47.0、Android SDK 36/build-tools ✓、CocoaPods 1.17、Xcode 26.6（已 switch+runFirstLaunch）；`flutter_copysync` 工程已创建；macOS debug 构建 ✅。网络：pub.dev 不通→`PUB_HOSTED_URL=https://pub.flutter-io.cn`；maven central/google 直连不通→`~/.gradle/init.d/aliyun-mirror.gradle`（settings 级镜像）+ 项目 `android/build.gradle.kts` 镜像仓库。
- 2026-08-24（kimi）网页基线（本地实例 127.0.0.1:15080，kimi-webbridge 真实浏览器 Chrome 操作，证据 `assets/baseline/web/` 13 张）：登录页（01）→登录后主界面双栏（容量条/在线设备数/设备传输区）（02）→上传按钮上传文件 200（03）→粘贴文本发送→按钮"发送中…"加载态→deliveries waiting（04）→图钉（pinned=1、永久，续期按钮消失，符合预期）（05）→续期 expires_at +7 天（06）→复制 toast（07）→搜索"baseline"（08）→图片类型筛选（09）→/go 线路选择页（10）→删除（原生 confirm() 二次确认，app.py:813）（12）→清理临时内容（confirm，图钉项保留）（13）。说明：webbridge 上传工具对隐藏 file input 被拒，上传/拖拽改用以页面真实 input change/drop 事件驱动（同一 JS 处理函数）；confirm() 弹窗用预置 window.confirm 桩解除（webbridge 无法应答原生对话框）。发现：桌面网页列表行无"备注"按钮（API `/api/items/{id}/note` 存在但无入口），Android 收件箱每项有"上传/复制"——矩阵已含备注行，V3 需补桌面入口。
- 2026-08-24（kimi）迁移演练（Task 19，commit 9478bb4）：线上 SQLite 在线备份（Python backup API，VPS 无 sqlite3 CLI）→本机副本迁移两遍幂等、items(50,800)/devices(3)/deliveries(48) 不变、四新表建成、旧代码回滚可读、71 测试全绿。发现：生产 50 项中 16 项已过期未被清理（线上 cleanup 依赖请求触发，阶段 5 前确认 cron）。
- 2026-08-28（阶段 5 收尾对账，纯只读复核，无构建/安装/发布）：对全部 `UNVERIFIED` 行按**阻塞根因**归三类，结论——真正阻塞 Phase 6 发布的仅 2 项，其余为证据完备性缺口，不阻塞发布：
  - **① 发布层/发布物问题（Phase 6 硬卡点，2 项）**：Mac 更新 row 63、Android 更新 row 79。两者**客户端更新链代码均健全且当前 fail-safe**（哈希不符即拒装；Mac 版本闸门另判「已最新」），缺陷都在**已发布的 artifact + manifest**（Mac 生产落后仓库 2.2/35 且 manifest 版本号漂移 2.0↔2.1；Android 发布物为错误应用 `xyz.copyweb`「AnyCopy」且清单 sha/versionCode 双不符）。修复动作 = **服务器侧重建并发布更新物**，属红线内、非本助手可执行，须 Zane 给发布方式/凭据。Android 另需生产签名密钥（现 release=debug 签名）。
  - **② 真机 GUI 门 — 受未签名 Debug 构建限制（5 项，Mac）**：主窗口快捷键 49、历史浮窗快捷键 50、区域截图 51、粘贴到前一应用 55、忽略下一次复制 56。代码+自动测试 PASS，仅因未签名 Debug 构建缺辅助功能/录屏权限无法在本地触发；解锁依赖**代码签名**（与 Phase 6 签名决策同源）。
  - **③ 真机 GUI 门 — 纯操作截图缺口（其余 UNVERIFIED，约 31 项：通用 20/21/22/23/24/25/26/27/29/30/31/33/34/35/36/38/40/41、Mac 52/53/54/58/59/60、Android 70/74/75/76/77/78/102）**：客户端代码健全且已被单元/集成测试覆盖，仅缺真人 GUI 操作留证；**不阻塞发布**，属发布后可补的证据完备性工作。
  - 一句话：Phase 6 只需先解 ①（发布物重建/发布 + Android 签名）与 ②（Mac 签名）两个签名/发布决策；③ 可并行或发布后补。全程仅只读复核，未改任何 artifact/manifest/`updates/` 原件，无 publish/deploy。
